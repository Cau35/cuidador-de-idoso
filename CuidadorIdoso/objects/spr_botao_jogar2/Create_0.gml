// Posição do botão na tela (mude esses valores como preferir)
x_gui = 100; 
y_gui = 100;

// Pega a largura e altura do sprite automaticamente
largura = sprite_get_width(spr_botao_jogar);
altura = sprite_get_height(spr_botao_jogar);