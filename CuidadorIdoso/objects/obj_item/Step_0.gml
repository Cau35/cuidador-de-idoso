// Se a variável 'arrastando' for verdadeira, o item deve seguir o mouse
if (arrastando == true) {
    // Atualiza a posição X e Y do objeto para a posição X e Y do cursor do mouse
    x = mouse_x;
    y = mouse_y;
}