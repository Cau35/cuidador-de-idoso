
porta_id = porta_id;

// === DESTINO ===
room_destino = room_destino;
porta_destino_id = porta_destino_id;       // ID da porta de chegada na room de destino — defina por instância

// === INTERAÇÃO (mesmo padrão do obj_npc) ===
raio_interacao = 150;
nome_acao = "Entrar"; // texto mostrado na dica do HUD

// === OFFSET DE SPAWN ===
// Onde exatamente o jogador aparece em relação à porta de chegada (evita nascer "dentro" da porta)
spawn_offset_x = 10;
spawn_offset_y = 50; // nasce um pouco abaixo da porta, por exemplo

// === REPOSICIONAMENTO DO JOGADOR AO CHEGAR ===
